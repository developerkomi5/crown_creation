//List of Enums
//it cannot be created inside a class

enum TextSizes { small, meduim, large }

enum OrderStatus { prpcessing, shipped, delivered }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm
}
